[build]
  base = "artifacts/omoggle-clone"
  command = "cd ../.. && corepack pnpm install --frozen-lockfile --ignore-scripts && corepack pnpm --filter @workspace/omoggle-clone run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
